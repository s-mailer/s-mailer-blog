source "https://rubygems.org"

# GitHub Pages provides its own supported set of Jekyll plugins (including
# jekyll-feed and jekyll-seo-tag), so pin the meta-gem rather than listing them
# separately to avoid version conflicts.
gem "github-pages", "~> 232", group: :jekyll_plugins
